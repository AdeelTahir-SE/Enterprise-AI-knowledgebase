have to set token in cookies for every request
have to implement redis that login user to be added to redis for fast handling of every request
have to remove unnecessary stuff
have to implement bloom filters (check if manually needed) so that no user with same name exists